language: go

go:
  - 1.12.x

install:
 - curl https://glide.sh/get | sh
 - glide install

deploy:
- provider: script
  skip_cleanup: true
  script: curl -sL https://git.io/goreleaser | bash
  on:
    tags: true
    condition: $TRAVIS_OS_NAME = linux